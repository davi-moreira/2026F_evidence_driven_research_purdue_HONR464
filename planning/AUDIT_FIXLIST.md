# AUDIT FIXLIST — Phase-7 → Stage-8 revision

**Source:** 11 independent Phase-7 auditors, synthesized and deduplicated.
**Date:** 2026-07-23 · **Drives:** the Stage-8 revision pass.
**Companion:** `planning/FINAL_REPORT_V2.md`.

## Tally

| Severity | Count |
|---|---|
| **CRITICAL** | **0** |
| **HIGH** | 11 |
| MEDIUM | 12 |
| LOW | 18 |
| **Total** | **41** |

**There are zero CRITICAL findings.** No defect on the ship-blocking tier. The 11
HIGH findings are concentrated and fixable in one focused pass; fix HIGH and the
two feasibility-density items before the course ships.

## Deduplication note — the biggest single work item

Five findings share one root cause: **the poster/conference layer and two
provenance docs were never migrated off the retired v1 M00–M23 numbering.** Handle
them as ONE reconciliation pass, not five edits:
`poster-evidence` H1 (9 protocol files, dangling links) · H2 (two competing poster
rubrics) · M-PE1 (`MILESTONE_PRESENTATION_MAP.md`) · M-PE2 (`schedule_data`
part3/part4 + "—" reading) · and `reproducibility` M-R2 (data-README stale
mapping). Rows are kept separate below because they touch different files, but
Stage-8 should schedule them together.

---

## CRITICAL

_None._

---

## HIGH (11) — fix before ship

| # | Area | Location | Problem | Fix |
|---|---|---|---|---|
| H1 | poster-evidence | `project/poster/*` + `project/conference/*` (9 committed protocol files) | Entire poster + conference PROTOCOL suite still on deprecated v1 M00–M23; every "Governs" link points to a milestone file that does not exist (7 dangling targets), plus wrong meeting labels/dates and the wrong flow (poster "locked at M12" vs v2 M10; Expo at "M17" vs v2 M12). These are the instructor's operating protocols for the poster/conference weeks — anyone running those weeks hits 404s. | v1→v2 reconciliation pass: renumber every header to real MWF labels + M0–M15 IDs and repoint each "Governs" link to the existing brief (storyboard/red-team → milestone_09 + _10; production → _10; presentation/uncertainty/hard-questions → _11; conference_reflection → _12; dress_rehearsal → _12/nb12). Or retire the suites (content now lives in nb09–nb12 + briefs) and drop the schedule-spine references. |
| H2 | poster-evidence | `project/poster/poster_rubric.md` vs `_research_project/2026Fall/milestone_10_final_poster_lock.md:218-229`; schedule pointer `scripts/schedule_data/part3.py:225,227,295` | Two different live 100-point rubrics govern the same terminal locked poster, and `poster_rubric.md` has NO AI-Research-Ledger row. The schedule tells students to self-score on `poster_rubric.md`; a student who scores Exemplary there and omits the ledger is then graded on milestone_10 and has the poster RETURNED unread with Craft 0 — a concrete student-harm path. | Make `milestone_10` the single source of truth: delete `poster_rubric.md` or replace its body with a one-line pointer to the milestone_10 rubric; update the `schedule_data` self-score references (part3.py:225,227,295) to cite milestone_10's criteria (incl. the ledger row). |
| H3 | consistency | `syllabus.qmd:187` (rendered into `docs/syllabus.html`) | The AI Research Ledger's 8th field is named **"responsible student"** here, but the canonical name everywhere students actually use it (course_config, CLAUDE.md, the ledger template, nb00, all eight briefs that print the field list, COURSE_MASTER_PLAN) is **"responsible researcher"**. | Change "responsible student" → "responsible researcher" in `syllabus.qmd:187`, then re-render `docs/`. |
| H4 | consistency | `syllabus.qmd:94,201`, `index.qmd:72`, `course_config.yaml:252`, `genai_studio/README.md:100-113` vs `milestone_06_*.md:23,207` and `milestone_08_*.md:180` | Every course-level statement says GenAI Studio reviewer consults are REQUIRED at exactly FOUR milestones (M5, M7, M9, M13), but the M6 brief makes the Experimental Design Reviewer a required graded consult and M8 requires an assigned reviewer red-team "part of the grade" — so required graded consults actually occur at SIX milestones. A student following the syllabus could lose points at M6/M8 for skipping a consult the syllabus said was required only at four other milestones. | Reconcile the count. Either (a) update the enumeration to six in syllabus/index/README (heading + table) and add M6 + M8 to `course_config` student_touchpoints; or (b) reframe M6/M8 reviewer use as optional support and remove it from the graded rubric lines (milestone_06:207, milestone_08:180). Make config, README, syllabus, index, and briefs agree on the exact REQUIRED set. |
| H5 | book-align | `book/.../01,05,06,08,23,24,28.qmd` (7 chapters) | Seven chapters link the verification guide with a relative path `[verification guide](../../ai_resources/verification_guide.md)`, but `ai_resources/` is not rendered into `docs/`, so the rendered href 404s on the published site. The other ~22 chapters use a working absolute GitHub blob URL — the flow is both broken and internally inconsistent. | Replace the 7 relative targets with the absolute URL the majority use (`https://github.com/davi-moreira/2026F_evidence_driven_research_purdue_HONR464/blob/main/ai_resources/verification_guide.md`), or render `ai_resources/*.md` and point all chapters at the `.html`. Standardize one form across all 37 chapters, re-render `docs/book`. |
| H6 | links | 7 student notebooks: `nb01,nb03,nb04,nb05,nb07,nb09,nb10` (sources in `_production_kit/nb_sources/*.py`, incl. stale `nb06_description.py`, `nb14_poster.py`) | Wrap-up "learn more" cells link `https://callingbullshit.org/tools/`, which returns HTTP 404 (correct live path is `/tools.html`, 200). Only broken external URL in the entire student-facing corpus. | Replace `callingbullshit.org/tools/` with `callingbullshit.org/tools.html` in the ~9 source `.py` files, rebuild affected notebooks with `nbbuild.py`, re-run `sync_instructor_repo.sh`. Optionally add `/tools/` to `audit_sources.py`'s blocklist. |
| H7 | accessibility | `project/poster/redteam_review_protocol.md` (four audits, no accessibility) + `project/poster/poster_rubric.md` (no accessibility criterion) | Accessibility is taught + drilled as a REQUIRED poster-review lens (nb10 cells 2/8/16; book ch27/28) but is absent from both graded instruments. A reviewer following the protocol gives zero accessibility feedback; an author who ignores color-blind/screen-reader readers loses no rubric points. What is taught is neither reviewed nor graded. | Add a 5th "Accessibility audit" block to `redteam_review_protocol.md` (no color-only distinction; color-blind-safe palette + redundant channel; direct value labels for screen readers; sufficient contrast) + a matching audit-slip line; fold an accessibility criterion into the poster rubric's Design & readability row (or a dedicated row). |
| H8 | genai-oversell | `nb15_managing_ai_agents_student.ipynb` vs `genai_studio/README.md:71-72`, `multi_model_comparison_lesson.md:133-135`, `roles/ai_research_team_orchestrator.md:19-20` | Three docs assert nb15 teaches the six-level capability taxonomy and levels 5–6 (autonomous/multi-agent) conceptually, so students can name why level-4 human sequencing differs from level-5/6 autonomy. But nb15 never mentions the taxonomy, "level", "autonomous", or "orchestration" (grep-confirmed). The course's central defense against agentic overreach is documented as delivered in the capstone but absent from it; a student could finish "Managing Multiple AI Agents" believing they orchestrated agents in an agentic sense. | Add one markdown cell to nb15 (near cell 4 or the wrap-up) stating the six-level taxonomy, marking this week level 4, naming levels 5–6 and that neither GenAI Studio nor Gemini is used at those levels, and qualifying "agents" in the title as human-run reviewer roles. (Or soften the three docs to "teaches the human-in-the-loop discipline" — but prefer adding the cell so the objective is delivered.) |
| H9 | genai-oversell | `ai_resources/agent_role_cards.md:13-19,132-138` vs `genai_studio/README.md:84`, `instructor_setup_guide.md:40-56`, `roles/ai_research_team_orchestrator.md:3,140-141` | `agent_role_cards.md` says only 5 roles live as GenAI Studio custom models and tags the other 8 (incl. the Orchestrator) as "Gemini prompted roles you create yourself in Colab" — flatly contradicting the rest of the suite, which builds all 13 as GenAI Studio custom models shared to the course group. A student/instructor gets contradictory build/consult instructions for 8 of 13 roles. | Reconcile `agent_role_cards.md` to the authoritative setup: mark every role available as a GenAI Studio custom model; if the 8 non-touchpoint roles MAY also run as Gemini-prompted in Colab, state "both homes" rather than tagging them exclusively Gemini-prompted. Fix the Orchestrator tag on line 132 to match its own spec. |
| H10 | feasibility | `PROJECT_MILESTONES.md:42-44` + `course_config.yaml` M13–M15 (190-192) / weeks 14-16 | Final stretch stacks three major deliverables in three consecutive weeks over a holiday: M13 (replicate + red-team a peer's ENTIRE package) due Sun Nov 29 over Thanksgiving; M14 (research note + reproducibility capsule) due Fri Dec 4 on a ~5-day live window (its nominal first develop meeting is the async module doing double duty); then terminal M15 (chapter + AI portfolio + defense) due Fri Dec 11. No slack, right after a break, for 5 novices. | Relieve the pile-up: push M14 due to Mon Dec 7 (window spans a weekend, not 5 weekdays), and/or reduce M13 to reproduce-only with red-team optional; post the M14 brief before break framed "over-break work is optional, real drafting starts Nov 30". Confirm the M14 develop window in PROJECT_MILESTONES.md:43 reflects that M37 is not a real drafting meeting. |
| H11 | feasibility | `course_config.yaml` week 9 (nb08/M8) + `MEETING_SCHEDULE.md` M23-M25 | Week 9 is the technical peak: nb08 (experimental causal — potential outcomes, randomization inference, blocking, clusters, factorials, seeded attrition/spillover sims, real numpy/pandas both lectures — the single most demanding notebook) lands the SAME week as M8, where novices produce their FIRST defensible computed estimate + interval on their own project (due Fri Oct 23). Hardest concepts + first hands-on analysis coincide for students with no quant background. | Decouple difficulty from first-analysis burden: move M8 one week earlier onto a lighter design week, OR hard-cap M8 for causal/experimental projects to a single one-number descriptive estimate. Ship a fill-in-the-blank worked Colab exemplar so the first estimate is scaffolded, not built from scratch. |

---

## MEDIUM (12) — fix in the same revision pass where cheap

| # | Area | Location | Problem | Fix |
|---|---|---|---|---|
| M-PE1 | poster-evidence | `MILESTONE_PRESENTATION_MAP.md:1-44` | Entirely on deprecated v1 M00–M23 and falsely claims to be "the authoritative ledger the validators check" (no validator reads it; `validate_milestones.py` validates M0–M15). Contradicts PROJECT_MILESTONES.md on poster lock (M12 vs M10) and Expo (M16 vs M12) and on M08/M09 presentation formats. | Rewrite to M0–M15 mirroring PROJECT_MILESTONES.md's chain table (or fold its presentation-variety/oral-ramp audits into PROJECT_MILESTONES.md and delete the file); drop the "authoritative ledger" claim. |
| M-PE2 | poster-evidence | `scripts/schedule_data/part3.py:91-92,125,193-228,259-296` + `part4.py:51-187` | The schedule spine cites stale v1 protocol files as "Other material"/"Provenance"/"the reading" for poster-criticism + conference weeks, sending instructors/students to the broken docs in H1. Separately, the poster-criticism week's `rdss_reading` is a parenthetical, so `update_schedule_badges.py:142-144` strips it to "—" — the LIVE student schedule ships that week with no reading shown. | Repoint every `project/poster`/`project/conference` reference to the corrected v2 protocol/brief/notebook; regenerate `MEETING_SCHEDULE.{csv,md}`. Give the poster-criticism + oral-defense weeks a real non-parenthetical reading value so the published schedule shows it. |
| M-C1 | consistency | `INQUIRY_MAP.md:117` | "Five pathways cover the space, plus prediction as its own objective" double-counts prediction (implies six). The space is the 2×2 (four cells); FOUR pathways cover it, prediction is the fifth. Its own table below lists five total, and nb00/nb01/config all say five. | Change line 117 to "Four pathways cover the space, plus prediction as its own objective (five in all):". |
| M-BA1 | book-align | `scripts/audit_sources.py:99` (glob scope) vs `book/**/*.qmd` | The citation-integrity gate scans only student notebooks; it never touches the agent-authored book chapters, which contain real citations and several narrated fake-citation illustrations. A fabricated/unretrievable citation added in a future chapter edit would ship uncaught. | Extend `audit_sources.py` (or `validate_book_sync.py`) to run the URL allowlist + fake-citation blocklist over `book/part*/*.qmd`; wire into the book render/CI. |
| M-A1 | accessibility | `nb04_observational_descriptive_student.ipynb` md cell 7 `<img>`; systemic across all 17 `<img>` tags in student notebooks | The one hand-authored embedded image (RDSS Fig 8.1) has no `alt` attribute (WCAG 1.1.1); the caption is not programmatically linked. Ironic given the course teaches screen-reader awareness. | Add descriptive `alt` to that image and all 17 `<img>` tags in the notebook sources, rebuild via `nbbuild.py`. Optionally add an audit rule failing on `<img>` without `alt`. |
| M-R1 | reproducibility | `nb08_experimental_causal_student.ipynb` SPILLOVER + CLUSTERING cells (`_production_kit/nb_sources/nb08_experimental_causal.py`) | Hidden cross-cell state: `tau = 4.0` is defined only in the earlier ATTRITION cell; SPILLOVER and CLUSTERING consume `tau` while framed as "a fresh clean world". Restart-and-run-all passes, but an isolated rerun of either cell (the tinkering the fresh-rng-per-cell design invites) raises `NameError`; spillover also prints `tau` so it fails on the print line. | Add `tau = 4.0` at the top of both cells (after each re-seeded generator), rebuild with `nbbuild.py nb08`, so each sim cell is genuinely self-contained. |
| M-R2 | reproducibility | `notebooks/data/README.md:16-20` ("Course use" column) | Shipped factual error: the per-notebook usage column uses stale v1 numbering and no longer matches which notebooks load each file (e.g. cliningsmith → nb05 not nb13; foos_etal → nb08+nb13 not nb13; lapop_brazil → nb01/04/09/14). An instructor using this doc is sent to the wrong notebooks. | Regenerate the "Course use" column by grepping `load_course_data("<file>")` across `notebooks/student/*.ipynb`. |
| M-SB1 | stem-breadth | `notebooks/data/` (all 5 CSVs) + every lab that calls `load_course_data` | Every dataset a student actually loads and computes on is social/political science (Brazilian political survey, voter turnout, GOTV experiment, Hajj lottery, BLM survey experiment). No bio/chem/eng/env/health data; the book teaches field-diverse examples but every linked lab routes to civic/poli-sci data, some politically charged. A bio/chem student reads their world in the concept then analyzes voter files in 100% of labs. | Ship ≥1 STEM teaching CSV (env sensor / agronomy field trial / materials coupon) and route 1–2 labs (nb04, nb08) to it; regenerate the dataset zip. If new data is out of scope, add one sentence per `load_course_data` cell framing the civic set as a neutral shared instrument applied to own-field data in the milestone. Prefer a field-neutral STEM set over the charged surveys for the most-reused example. |
| M-F1 | feasibility | `course_config.yaml` milestones M0–M15 | 16 graded milestones across 16 consecutive weeks with ZERO catch-up/slack week, on a cumulative dossier where each feeds the next. One novice slip cascades with no recovery point. | Designate one lighter recovery point (natural candidate: the week after the M8 peak) as a no-new-milestone consolidation/catch-up studio, or explicitly document per-milestone that deliverables are individually small so the cadence is calibrated. |
| M-F2 | feasibility | `MEETING_SCHEDULE.md` M20, Fri Oct 9 | The Oct 9 studio (a hard anchor) carries THREE graded events in one 50-min period the week before Fall Break: M6 due + URC abstract internal gate + M7 kickoff; the guide's own contingency ("post the kickoff as homework if the clinic overruns") concedes it doesn't reliably fit. | Decouple the URC abstract internal gate from the M6 due date — move it to the prior Wednesday (M19, Oct 7) or make it a soft async check — so Oct 9 is a single-deliverable studio. Also de-risks the hard external URC anchor. |
| M-F3 | feasibility | `syllabus.qmd` (no workload/credit section) + most milestone briefs (no effort budget) | For a course whose defining feature is a graded deliverable every week, there is no credit-hour context or expected weekly out-of-class time anywhere, and most briefs give no length/effort budget. Neither students nor instructor have a calibration anchor. | Add a credit-hours + expected-weekly-out-of-class-hours line to `syllabus.qmd`, and a one-line "typical effort / expected length" budget to each brief (via the brief template so it propagates). |
| M-GO1 | genai-oversell | `multi_model_comparison_lesson.md:128-136` vs `nb15` | The doc claims its protocols are "used in Week 16 (nb15)" and "You run all three protocols as part of the AI-management capstone," but nb15 contains no multi-model comparison activity and never references the lesson (grep-confirmed). An instructor expects a run that isn't there. | Either add a short multi-model comparison activity to nb15 (Protocol A on a real project claim) and cite the lesson in nb15's provenance, or correct the doc to say the lab runs in nb09 and is an optional companion in Week 16 (remove "You run all three protocols" for nb15). |

---

## LOW (18) — polish; batch opportunistically

| # | Area | Location | Problem | Fix |
|---|---|---|---|---|
| L1 | consistency | `course_config.yaml:253` (M9) | Role abbreviated "Robustness Reviewer"; canonical name is "Robustness & Sensitivity Reviewer" (role file + brief + README). Config is the machine-readable source of truth. | Change to "Robustness & Sensitivity Reviewer". |
| L2 | consistency | `COURSE_MASTER_PLAN.md:42` | Ledger field 2 rendered "tool"; canonical field name is "Tool used". Minor compressed-list drift in a source-of-truth planning doc. | Use "tool used" (or leave as intentional shorthand — low priority). |
| L3 | book-align | `book/.../07.qmd:58`, `01.qmd:44`, `08.qmd:63` | To teach hallucination-catching, chapters narrate specific fake sources ("Journal of Green Chemistry", "ASTM E-4471", a fabricated 2021 biochar cite). Each is immediately debunked and none presented as real — NOT a fabricated-cite-as-real defect — but naming specific fake titles sits in tension with CLAUDE.md D16. | Instructor eyeball: confirm acceptable under D16 (reads compliant). If strict, describe the shape of a hallucinated cite without naming a specific fake title, and add any retained names to the `audit_sources` allowlist so the gate stays green when extended to the book. |
| L4 | links | `book/_quarto.yml` / `docs/book/index.html` | The Quarto book is a standalone project with no navigation link back to the main course site — one-way navigation; a reader can only return via browser back button. | Add a back-link to the course site in `book/_quarto.yml` (navbar/sidebar entry to `../index.html` or the absolute github.io URL), re-render the book, commit `docs/book/`. |
| L5 | accessibility | `nb12_conference_student.ipynb` code cell 11 routing diagram (`node_colors`) | The "CLAIMS signal" node is solid #4C72B0 with black label; measured contrast ≈4.3:1, just under WCAG AA 4.5:1. nb04/nb08 DAG nodes use light #DCE6F1 and are fine; only nb12's darker node is borderline. | Fill labeled nodes with light #DCE6F1 (as other notebooks) keeping black labels, or use white label text on the dark nodes, so every label clears 4.5:1. |
| L6 | reproducibility | `notebooks/data/cliningsmith_etal.csv` + nb05 loader + README/zip | Filename misspells the author surname: study is Clingingsmith, Khwaja & Kremer; file drops the first "g". README prose spells it correctly — a filename/variable inconsistency on the attribution surface. | Rename to `clingingsmith_etal.csv` and update the nb05 loader, README table, and zip atomically in one commit (keep the raw URL resolving); OR leave as-is and add a one-line "filename spelling" note in the README. |
| L7 | reproducibility | `load_course_data` fallback (all notebooks) vs `honr46400_datasets.zip` layout | Offline path gap: the zip extracts CSVs to a `honr46400_datasets/` folder, but the loader's local fallback only tries `notebooks/data/<file>` and `../data/<file>`. A student who extracts only the zip and runs offline hits FileNotFoundError. | Add `Path("honr46400_datasets")/filename` and `Path(filename)` to the fallback chain (edit shared snippet, rebuild all notebooks), OR document in the README that offline use requires placing CSVs under `notebooks/data/`. |
| L8 | reproducibility | `requirements.txt` + setup cells vs `environment_documentation.md:109` | (1) `requirements.txt` lists only numpy/scipy/jupyter; notebooks also need pandas/matplotlib/networkx/scikit-learn/scipy for a local run, only networkx has an inline pip hint — a local run of nb07/nb09 can fail with no guidance. (2) The reproducibility package's quality bar says "the notebook prints its Python version," but exemplar setup cells print only the seed. | Add a `requirements-notebooks.txt` pinning pandas/matplotlib/networkx/scikit-learn/scipy; add `import sys; print('Python', sys.version.split()[0])` + a numpy/pandas version print to setup cells so they model the taught habit. |
| L9 | stem-breadth | `book/.../16.qmd:53`, `26.qmd:51`, `36.qmd:63` | Three of the four physics worked examples are the same "measure g with a pendulum" vignette (ch16/26/36); physics coverage is narrower than it appears. | Vary at least one instance to a different physics context (optics diffraction, thermodynamics cooling, RC time constant). Deliberate running-example through-line, so polish not defect. |
| L10 | poster-evidence | `milestone_10_final_poster_lock.md:76-104,200-214` | v1 sanctioned a thin-pilot "design + preliminary evidence" poster (honest path for undergrads whose IRB/external data may not land by Nov 6); v2's terminal brief drops that fallback and assumes a headline number + interval. A project with no runnable data by Nov 6 has no sanctioned poster path. | Add a short "design + preliminary evidence" clause to milestone_10 mirroring M8's public-dataset-stand-in language: a data-delayed project may present a design-and-preliminary-evidence poster whose headline honestly claims the design, keeping the reproducing-number gate real without stranding a delayed project. |
| L11 | poster-evidence | `milestone_08:199-207`, `_09:183-193`, `_10:231-245`, `_12:73-97` | The M8/M9/M10 evidence caps are per-row penalties, not gates: a non-reproducing headline number caps Verification at Beginning but does not block submission, and the Expo is required regardless — so a project can reach the poster/Expo carrying an unverified number (~60/100 and still presenting). | If a hard floor is intended, add an explicit gate to milestone_10 (a non-reproducing headline number may not be locked/submitted until it reproduces, mirroring the ledger "returned unread" mechanic). If penalty-not-gate is intended, state that once in `ASSESSMENT_ARCHITECTURE.md` as deliberate policy. |
| L12 | adv-ai | `ms01,ms04,ms08,ms09,ms10` (both embedded prompts each); sharpest at ms08 cell 4 | These 5 milestone notebooks label the "After running, verify" block generically as "(the responsible-AI habit)", while all 16 lecture notebooks + the other 11 milestones name the specific AI-failure archetype the check counters. Still gated (verify items concrete) — consistency polish, not a gating hole. | Replace the generic tag on the two prompts in each with the named archetype it counters (e.g. ms08 cell 4 → "counters *plausible-but-wrong-method*"). |
| L13 | adv-ai | Project-Transfer cells (nb15 c49, nb09 c55, nb06 c57, nb01 c53) | The Project Transfer cells (which assemble the milestone spine) are gated only by instruction + a preceding AI-free checkpoint, with no in-notebook commit-first structure like Pause & Predict — a student could paste AI output and rely on the downstream oral defense as the human-only gate. Existing red-team + defense already covers this. | Optional hardening: add a one-line "commit your own draft in this cell before opening any AI" cue to the Project Transfer cells. No change required to ship. |
| L14 | adv-ai | nb09 c17→18, nb06 c18→19, nb06 c15 | Worked-interpretation prose in "Reading the Evidence" cells states part of the answer the immediately-following graded YOUR ANSWER cell asks for (e.g. nb09 c17 states the headline "≈0.63 SD … to ≈0.90 SD" right above the cell asking for that headline); a student can transcribe rather than reason. nb06 c15 also pre-prints a live-reveal punchline. | Soften the prose to model the reasoning move without stating the final one-sentence claim; move nb06 c15's conclusion (SE≈0.022) into the post-run cell so the live reveal isn't pre-printed. |
| L15 | adv-design | `book/.../14-prediction-and-generalization.qmd:176` | Glosses cross-validation as "the spread you see when you repeat the held-out split several times" — that describes repeated random subsampling, not k-fold (whose defining feature is every case tested exactly once). The chapter's own lab nb07 §4 runs proper 5-fold. Front-matter looser than and mildly inconsistent with its lab. | Reword to name the k-fold mechanism ("splits the data into folds and rotates which fold is held out so every case is tested exactly once"), matching nb07 §4. If the repeated-split idea is wanted, label it "repeated hold-out". |
| L16 | genai-oversell | `genai_studio/colab_api_poc.md:112-118` (Cell 2) | The doc's intro promises the sequence "fails gracefully" and Cell 3 never dumps a traceback, but Cell 2 fetches + parses each role prompt with no try/except — a network failure/404/format shift raises a raw traceback instead of pointing to the guaranteed manual-UI path, on the API-assumed path. | Wrap `load_system_prompt`'s fetch-and-parse in try/except that, on any failure, prints a plain message directing to the manual-UI fallback (mirroring Cell 3). |
| L17 | feasibility | `scripts/schedule_data/__init__.py:9`; `CALENDAR_BACKBONE.csv` no_class 2026-11-18 | Nov 18 is an instructor-declared cancellation ("post-Expo Wednesday"), not a Purdue holiday — an ad-hoc contact-hour reduction on a normal MWF day; may need documented justification or makeup if Purdue enforces contact minutes. | Confirm Nov 18 against the official Purdue Fall 2026 calendar; if not a recognized non-instructional day, document it as a deliberate post-Expo recovery day with contact-hour rationale (or restore it and lighten that week another way). |
| L18 | feasibility | `PROJECT_MILESTONES.md:41,58`; `course_config.yaml` events.urc_expo | The URC Expo (Tue Nov 17) is a required graded component of M12 (present + evaluate ≥3 posters) on a Tuesday — a day the MWF section never otherwise meets — an out-of-pattern required time block whose expected commitment is not surfaced in the student-facing syllabus/schedule. | State the Expo time commitment explicitly in the syllabus + schedule page (approx setup + presentation + peer-evaluation window on Tue Nov 17) so students treat it as a fixed required event from Week 1. |

---

*Ranked most-severe first. Zero criticals. Fix all HIGH plus feasibility H10/H11 (density) before ship; sweep MEDIUM/LOW opportunistically in the same pass. After fixes: re-run the full gate battery, re-render `docs/` (site + book), commit, push, re-sync the instructor repo.*
